{
  "name": "flowerst",
  "version": "1.0.0",
  "description": "Flower St – E-commerce website with Google Sheets catalog sync",
  "private": true,
  "scripts": {
    "fetch-catalog": "node scripts/fetch-catalog.js",
    "start": "npx http-server src -p 3000 -o --cors",
    "build": "node scripts/fetch-catalog.js",
    "deploy": "node scripts/fetch-catalog.js && echo 'Ready to deploy src/ folder'"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
